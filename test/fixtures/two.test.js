const { test } = require('tap');

test('alpha', async (t) => {
  t.equal('one', 'one');
  t.equal('two', 'two');
});

test('bravo', async (t) => {
  t.equal('one', 'one');
  t.equal('two', 'two');
});
